{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd brand-house && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd brand-house && npm run start"
  }
}
