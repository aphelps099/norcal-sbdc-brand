import BuilderShell from "@/components/builder/BuilderShell";

export const metadata = {
  title: "Page Builder — NorCal SBDC",
};

export default function BuilderPage() {
  return <BuilderShell />;
}
